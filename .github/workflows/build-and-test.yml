name: Build Nadhi 
run-name: ${{ github.actor }} is building out Nandhi 🚀

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - `*`


# inputs:
#   ros2-distro:
#     description: 'The ROS 2 distribution to be used.'
#     required: false
#     default: 'humble'

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}

      - name: Setup dependencies
        run: bash ${{ github.workspace }}/.github/workflows/setup/ros2-install.bash rolling
        shell: bash
      
      - name: Build workspace
        run: source /opt/ros/rolling/setup.bash && python3 aadesh.py build all
        shell: bash
